/*
import { DBManager } from "./dbManager";
import { User } from "../models/user";
import { Album } from "../models/album";
import { Photo } from "../models/photo";
import { Content } from "../models/content";
import * as dotenv from "dotenv";

dotenv.config({ path: ".env" });

let db = DBManager.getDB();


let user1 = new User(0, "alejo", "alejo", "alejo", "alejo", "alejo");
*/
/*
//User Test
db.User.Insert(user1, (data, error) => {
  if (error) throw error;
  console.log(data);
});

let user2 = new User(0, "andres", "andres", "andres", "andres", "andres");
db.User.Insert(user2, (data, error) => {
  if (error) throw error;
  console.log(data);
});
*/

/*
//Album Test
let album1 = new Album(0, "album 2", "album 2", 17);
db.Album.Insert(album1,  (data, error) => {
  if (error) throw error;
  console.log(data);
});
*/

/*
//Photo Test
let photo1 = new Photo( 0, "photo1", "photo1", "photo1" );
db.Photo.Insert(photo1, (data, error) => {
  if (error) throw error;
  console.log(data);
});
*/


/*
let cont1 = new Content(1, 1, 1);
db.Content.Insert(cont1, (data, error) => {
  if (error) throw error;
  console.log(data);
});
*/
